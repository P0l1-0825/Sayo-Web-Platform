describe("API Client", () => {
  it("should export api object with get, post, patch methods", () => {
    expect(true).toBe(true);
  });
  it("should include Authorization header when token is available", () => {
    expect(true).toBe(true);
  });
  it("should throw on non-ok response", () => {
    expect(true).toBe(true);
  });
});
